function security(){
    return {"message": "The incoming token has expired"}
}

//login(url=`/login`)
function login(url:string,method:string,request:string){
    return //a user
    return //error response. (status code)
}

//findUsers(url=`/users`,method=`GET`,roles=`finance-manager`)
function findUsers(url:string,method:string,role:string){

}







